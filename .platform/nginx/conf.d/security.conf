# Block common attack patterns
if ($request_uri ~* "(eval-stdin|phpunit|/cgi-bin|\.env|thinkphp)") {
    return 444;  # drops the connection without logging
}

# Block specific IPs (replace with IPs from your logs)
deny 114.220.75.156;
deny 185.242.226.114;
deny 205.185.107.72;

# Optional: block suspicious User Agents
if ($http_user_agent ~* "(libredtail-http|curl|wget|python-requests)") {
    return 444;
}
